"""Reusable visual building blocks.

Each module owns one domain concept and exposes VGroup subclasses that a scene
composes. Check this package before writing a new visual — see the promotion
rule in the root AGENTS.md.
"""
