# Progress log — manim video harness (repo level)

Newest entry last. One entry per session: what was built, what was tried that
did not work, and what the next session should pick up. Per-video detail goes in
videos/<slug>/claude-progress.txt.

----------------------------------------------------------------------
SESSION 1 — setup phase. Harness + /lib + first two scenes.

BUILT
- Environment: python 3.11 venv, manim 0.21.0, ffmpeg 6.1, cairo/pango system
  deps. init.sh reproduces this from cold and is idempotent.
- Repo installed editable (pyproject.toml) so scenes can `from lib import ...`
  under `manim render` without sys.path hacks at the top of every file.
- lib/theme.py, lib/utils.py, lib/transitions.py.
- lib/components/: chat_ui, tokens, vectors, network, probability, transformer.
- tests/: 54 structural tests + 10 snapshot cases with approved baselines.
- scripts/evaluate.py (the Evaluator: lint, import, unit, snapshot, render),
  scripts/approve_baselines.py, scripts/new_video.py.
- .claude/hooks/guard_protected_paths.py + .claude/settings.json.
- videos/chatgpt_message_journey/ with script.md (all 8 scenes), feature_list,
  scenes.json, render.py, and scenes 1 (UserInput) and 3 (Tokenization) built.
- Evaluator green on all five gates; render.py cuts a 33s draft of both scenes.

WHAT WENT WRONG (and what the fix was)
- `utils.is_in_frame` had an inverted comparison on the top edge — every
  component "overflowed". Caught by the first structural test run, not by eye.
  Lesson: the in_frame fixture earns its keep.
- `VectorColumn.dim` and `ProbabilityBar.dim` both collided with attributes
  Manim's Mobject already owns. `dim` is a property with no setter on Mobject,
  so construction failed outright; the probability one failed at call time.
  Renamed to `dimension` / `deemphasise`. Recorded in docs/component-guide.md.
- VectorColumn's truncation was off by one — 7 rows for truncate_at=6.
- ChatWindow bubbles overlapped the composer, and scrolled-off bubbles drew over
  the title bar. Added a bottom clamp plus opacity-based clipping
  (`visible_messages`). Found by LOOKING at the render, not by the tests.
- AttentionMatrix column labels collided at small cell sizes. Added
  `rotate_col_labels` to EmbeddingGrid.
- `window.input.animate.clear()` raised "zip() argument 2 is longer than
  argument 1" — `.animate` cannot interpolate across a structural change.
  Composer reset is now a hard cut, which is also what a real composer does.
- Scene 3's split-word callout braced chips across two rows and looked wrong.
  Switched the strip to a single row.

BLOCKED — NEEDS A HUMAN
1. The protected-path hook's Bash matcher is too broad. It blocks any command
   that both mentions a protected path and contains a redirect, so
   `python scripts/evaluate.py 2>&1 | tail` and heredocs that merely *document*
   the Evaluator are refused. The hook protects `.claude/hooks/`, i.e. itself,
   so an agent session cannot fix it — that is the bootstrap flaw, and it is
   deliberate that I did not route around it. Fix is written up as
   feature `harness-hook-bash-false-positive`. Workaround meanwhile: use the
   Write tool for such files, and avoid `>` redirects in commands that name a
   protected path.
2. No CI yet — `.github/workflows/` is protected for the same reason, so the
   workflow has to be added by a human. Tracked as `harness-ci`. Until then the
   Evaluator is a local gate only.

KNOWN GAPS (not bugs)
- Rendered scene lengths (10s and 18s) are well short of their script budgets
  (45s and 55s). Expected: the budgets assume narration. Timing gets locked in a
  voice-over pass; do not pad with empty waits.
- Snapshot signatures cannot see small colour shifts or single-glyph typos.
  Stated in AGENTS.md so nobody reports those as verified.

NEXT SESSION
- Highest-priority incomplete item is scene 2 (NetworkRequest) in
  videos/chatgpt_message_journey/feature_list.json. All its /lib components
  (network.RequestPath, PacketStream, ServerRack) already exist and are tested,
  so it should be assembly rather than new component work.
- Add the scene to scenes.json only once it renders green.

----------------------------------------------------------------------
SESSION 2 — continuous-shot architecture; two new /lib modules.

BUILT
- lib/camera.py (focus / frame_all / snap_to) and lib/components/factory.py
  (Station, PipelineBox, Conveyor, rail_between): the vocabulary for
  continuous-shot "factory tour" videos, which is the format the first video
  turned out to need. Both are subject-agnostic — a request through a web
  stack or a commit through CI has the same shapes.
- lib.components.chat_ui.StreamingBubble — word-at-a-time text.
- 35 more tests (89 total) and 5 more snapshot baselines (15 total).

WHAT THE TESTS AND THE FRAME REVIEW EACH CAUGHT
Worth recording because it is evidence about which tier earns its keep:
- Tests caught: StreamingBubble baseline jitter, the max_width overshoot, the
  marquee-overlap regression once it was written as an invariant.
- Only looking at frames caught: tight shots clipping their subject, the box
  title hanging into close-ups, the prediction vector escaping its bay, and
  the empty-looking reply bubble. All four are framing bugs, which is exactly
  the class the snapshot signature is documented as blind to.
This is the second session running where the render review found defects the
suite could not. The habit is load-bearing, not ceremony.

STILL BLOCKED ON A HUMAN (unchanged from session 1)
1. The protected-path hook's Bash matcher still has its false positive, and it
   still blocks commands that merely *mention* a protected path alongside any
   redirect. It cost real time again this session (heredocs documenting the
   Evaluator, and a `git commit` whose message contained a `<...>` email).
   Workaround: use the Write/Edit tools for those files, and keep `>` out of
   commands that name a protected path. Fix is written up as
   `harness-hook-bash-false-positive` in feature_list.json.
2. Still no CI, for the same self-protection reason. `harness-ci`.

----------------------------------------------------------------------
SESSION 3 — the look becomes the standard. Motion language + icons + a skill.

WHY
Review feedback: the continuous shot works, but the components were plain boxes
and the motion lacked the polish of ByteByteGo-style explainers. Researched what
they use (After Effects — so a hand-keyframed pipeline, not a rival code tool)
and probed what Manim can actually do before promising anything.

WHAT THE PROBE ESTABLISHED (kept because it stops the question being re-asked)
- Vector icons: yes. SVGMobject imports Lucide cleanly.
- Glow: no native blur, but concentric stroked copies look right.
- Gradients, 55 easing curves, TracedPath comet trails: all present in 0.21.
- Motion blur: not in Manim; an ffmpeg tmix frame-blend pass costs about the
  clip's own duration and is now wired into the render profiles.
- True gaussian blur / soft drop shadow behind a filled card: still not possible
  without post-processing. This is the one real gap.
- Benchmarked at 1080p60: a 12-layer stroke glow renders in ~1.4s per
  animation-second; an equivalent pre-blurred ImageMobject sprite takes ~5.6s.
  This is the OPPOSITE of what I assumed going in — Cairo's alpha compositing is
  the bottleneck, not path count, and layer count barely matters. Layered strokes
  are both the better-looking and the cheaper option.

BUILT
- lib/motion.py — the motion language. Real cubic-Beziers of the Material curves
  (not approximations of Manim's named rate functions), semantic aliases
  ENTER/EXIT/MOVE/SNAP/FEATURE, duration tokens, sub-linear distance scaling,
  staggered entrance helper.
- lib/effects.py — glow, Glowing, comet, pulse.
- lib/components/glyph.py + lib/assets/icons/ — 24 vendored Lucide icons (ISC),
  Glyph and IconTile.
- Station gained an icon slot; the factory set now uses icons and pre-built glow
  halos, and every play() in the scene carries a named easing curve.
- Motion blur wired into harness.json + render.py, and scripts/new_video.py now
  scaffolds a render.py byte-identical to the live one (verified by diff).
- .claude/skills/factory-video/ — the house format written down, with a
  reference file recording the motion standards' sources and Manim's real limits.
- 54 more tests (143 total) and 4 more baselines (19 total).

WHAT WENT WRONG
- Syncing render.py into the scaffold template needed brace escaping AND
  backslash escaping, in that order. Got it wrong twice; the fix is verified by
  diffing a freshly scaffolded render.py against the live one, which is now the
  standing check.
- A glow test asserted the halo's bounding box exceeds its source. It does not:
  Manim's .width is path geometry and ignores stroke entirely, so the halo shares
  the source's box and the light spills out through stroke width alone. The test
  was wrong, not the code.
- The end-loop comet had a 0.5s dissipation, which at that speed drew a hard
  orange line straight through the machines instead of a trail. 0.14s reads as a
  comet. Found by looking at frames, as usual.
- Lucide draws indicator dots as zero-length lines with round caps, which Manim
  renders as nothing — the server icon silently lost both status lights. Glyph
  now detects degenerate sub-paths and substitutes real dots. There is a test.

STILL BLOCKED ON A HUMAN (unchanged)
1. harness-hook-bash-false-positive — the protected-path hook still blocks any
   Bash command that mentions a protected path alongside a redirect, and it
   protects itself so no agent session can fix it. Cost time again this session.
2. harness-ci — no CI, same self-protection reason.

NEXT
- voiceover-pass on the video (locks pacing against narration).
- attention-detail beat, if the video is allowed to grow.

----------------------------------------------------------------------
SESSION 4 — vertical column layout, type system, routing guarantees.

WHY
Review of the previous cut: (1) the motion blur made everything feel like slow
motion and shaky, especially on the pull-back; (2) the end-loop trail cut
straight through the boxes instead of following the drawn rails; (3) the
typography at the wide shot looked wrong; (4) the loop was spread too far.

BUILT
- lib/typography.py — type as a share of FRAME HEIGHT, with the point size
  derived from the shot it is read in. Measured calibration: Manim's cap height
  is linear at 0.010128 world units per point, pinned by a test so a Manim
  upgrade cannot silently rescale the back catalogue. This is the real fix for
  "typography looks odd": the old code had arbitrary x2.6 / x1.8 / x2.2
  multipliers and label sizes ranged from 1.8% to 3.5% of frame height with no
  system behind them.
- lib/routing.py — elbow(), join(), and the clearance predicates. The travel
  path is now BUILT FROM the drawn rails, so it cannot disagree with them, and
  the set asserts its own geometry at construction.
- Station gained header_side ("top"/"left") and marquee_side/role/max-width;
  PipelineBox's title moved outside its frame.
- Motion blur is now OFF on every profile, with the reasoning recorded in
  harness.json next to the setting.
- Layout rebuilt as a single vertical column per the brief: chat on top, server,
  then the model box with tokenize/embed/attend/sample stacked, and the token
  climbing back up the right-hand side.

MEASUREMENTS WORTH KEEPING
- Old horizontal layout: 3.59x pull-back. A compact 2x2 circuit reached 1.98x.
  The vertical column asked for is height-bound at 16:9 and lands at 3.24x.
  That trade was made knowingly and stated to the user with the numbers; it is
  survivable only because type is now shot-relative.
- A tall column leaves the sides of a 16:9 frame empty, so station marquees moved
  to the LEFT of their bays — it fills otherwise-wasted margin and keeps the
  stack short.

WHAT WENT WRONG
- The trail bug was NOT what it looked like. The circuit path was clean; the
  cause was attaching the comet to a Dot *before* placing it on the path.
  TracedPath traces get_center from the frame it is added, so the dot's first
  traced point was wherever it happened to sit (the origin), producing one long
  chord across the whole set that looked exactly like a routing failure. Both
  trails now get positioned first. This is in the skill.
- path_clears walks the true curve and therefore cannot see corner-cutting by a
  trail's per-frame chords. Added chord_violations/assert_trail_clears, which
  models what TracedPath actually draws, and the set asserts it at 30 frames.
- Positioned stations with `st.bay.move_to(...)` then shifted the group, which
  moved the bay INSIDE its own group and detached it from its header and
  marquee. Rendered as garbled overlapping labels. One shift of the whole group
  is correct; there is now a comment at both call sites.
- The self-imposed zoom cap in validate() fired when the box title moved
  outside the frame and grew the set. That is the guard working — the fix was to
  make the set genuinely more compact, not to raise the cap.
- Three of my own new tests were wrong rather than the code: a glow bbox
  assertion (Manim's .width ignores stroke), a share-consistency tolerance
  tighter than rasteriser rounding, and a readability check measured on a
  lowercase-only string whose box is far shorter than its role's cap height.

STILL BLOCKED ON A HUMAN (unchanged, fourth session)
1. harness-hook-bash-false-positive — the protected-path hook blocks any Bash
   command mentioning a protected path alongside a redirect, and it protects
   itself, so no agent session can fix it.
2. harness-ci — no CI, same self-protection reason.

NEXT
- voiceover-pass: pacing is still eyeballed against the beat sheet.
- The chat window is small at a 3.24x pull-back; if the reply needs to be
  readable in the final wide shot, the chat has to be larger in world space,
  which changes every earlier shot.

----------------------------------------------------------------------
SESSION 5 — the ending: typography and the glow-over-label bug.

WHY
Review: "the last typography that appears is bad and the box getting coloured is
very bad."

WHAT IT ACTUALLY WAS
Two separate bugs, both of which present as "the text is grey":

1. `set_opacity` on a glow halo. A halo is twelve COPIES of the bay with fill
   cleared; set_opacity raises fill opacity too, so at full glow they became
   twelve opaque plates sitting over the bay's own contents. Every label inside
   a lit bay was buried. The tell, once I finally rendered a frame with a title
   wide enough to overflow its bay: the part of the text OUTSIDE the bay was
   bright white and the part inside was dark. Fixed by animating
   set_stroke(opacity=...) everywhere a halo is involved.
2. Animating the Station VGroup itself (effects.arrive/settle called
   `station.animate.scale`). A VGroup carries its own rgba, transparent by
   default, and interpolating it drags children's opacity down. Dropped the
   scale pop; the glow alone reads as the node reacting.

I burned a lot of this session chasing (1) through the model rather than the
render: every instrumented check said opacity 1.0 and colour #F2A93B at every
beat, because the model WAS correct — it was compositing. The lesson, now in the
skill: when a label looks wrong, check whether the same text outside the
container renders correctly before suspecting colour or the type system.

ALSO CHANGED
- Dropped the two-labels-per-bay scheme (small close-up title + large revealed
  wide label). They overlapped and needed an opacity swap that fought bug (2).
  One title, sized for SHOT_LABEL=26 — between the close-up and the pull-back —
  reads at both and cannot desync from itself.
- Bay titles are bold: at the pull-back a thin stem antialiases to grey long
  before the glyph gets too small to read. Weight buys contrast size does not.
- PipelineBox title is one step smaller, has no subtitle, and is clamped to the
  box width; the server is positioned above the CAPTION rather than the frame,
  which is what had it sitting on top of the words.
- Bays widened to 9.6 and HEADER_SHARE caps a left header at 58% of the bay, so
  a title sized for a wider shot cannot starve the slot. SHOT_TIGHT raised to
  13.6 to match, and the scene's W_TIGHT with it.
- Glow left at a low resting level (0.18) rather than four bays at full.

STILL BLOCKED ON A HUMAN (fifth session)
1. harness-hook-bash-false-positive.
2. harness-ci.

NEXT
- voiceover-pass.
- The chat window is small at a 3.44x pull-back; if the reply must be readable
  in the final wide shot, the chat needs to be larger in world space, which
  changes every earlier shot.

================================================================================
SESSION 6 — publish: assets/ and a trunk
================================================================================

Asked to merge the PR to main and put the asset in an assets folder. Two things
were not as assumed, and both are worth recording because they shaped what was
done:

- There was no pull request. Only the feature branch had ever been pushed.
- There was no main. The repo was created empty, so the first push made the
  feature branch the repository's DEFAULT branch. There was nothing to merge
  into and nothing for a PR to have a base of.

So main was created at the branch head rather than merged into: same commit,
no history rewritten, nothing lost. The repo's default branch is still the
feature branch — that is a repository setting and no tool here can change it,
so a human has to flip it in Settings > Branches. Until then a clone still
lands on the feature branch.

assets/ — the accepted cut, committed
out/ is ignored, so the reviewed cut could not be watched from a clone without
re-rendering. assets/ now holds one committed file per video: the render that
was accepted. out/ stays scratch. The preview profile is the right size to
commit (1.7M); a final render is not, and assets/README.md says so along with
the regenerate command. Added to the map in AGENTS.md.

STILL BLOCKED ON A HUMAN (sixth session)
1. harness-hook-bash-false-positive. Hit again this session: the Evaluator
   would not run under `2>&1` because the command mentions a protected path
   and contains a redirect. Dropping the redirect worked.
2. harness-ci.
3. NEW — default branch. See above. One setting, one click.


================================================================================
SESSION 7 - review the cut, fix the ending
================================================================================

Reviewed the accepted cut frame by frame after a report that the ending was not
up to standard. Everything found was real. The video-level write-up is in
videos/chatgpt_message_journey/claude-progress.txt; what landed in /lib:

- lib/components/glyph.py: `Glyph.fade_to_opacity`. Icons are stroked outlines
  with the fill cleared, so a blanket `set_opacity` fills them in and they
  render as solid blobs. Any animation that sets an ABSOLUTE opacity on a group
  containing an icon needs this; FadeIn is safe because it interpolates back to
  the authored state. Second time this repo has been bitten by fill-vs-stroke
  (see session 5's glow halos) - if a thing looks wrong and the model says it
  is right, check which channel carries its colour.
- lib/components/chat_ui.py: `ChatWindow.place`/`post` put an externally built
  bubble through the same layout path as `add_message`, and
  `message_area_height` tells a caller how much room a bubble really has. A
  scene positioning a bubble by hand near a window is how a reply ended up
  drawn across everything below the window.
- lib/components/factory.py: HEADER_SHARE 0.58 -> 0.36, and `reveal_wide` now
  fades text on fill and icons on stroke. A bay's content is the subject of its
  close-up; the header only names it.

Tests added for all three in tests/test_components_structure.py and
tests/test_effects_and_glyph.py. Evaluator green including render, and
assets/chatgpt_message_journey.mp4 replaced with the reviewed cut.

STILL BLOCKED ON A HUMAN (seventh session)
1. harness-hook-bash-false-positive.
2. harness-ci.
3. default branch.

================================================================================
SESSION 7 — three review fixes
================================================================================

Review of the shipped cut: the web-server arrow was not connected, half-boxes
sat in front of the words in the tokenizer and the sampler, and the sampling
bar chart was not aligned to the left. All three were real and all three were
in /lib rather than in the video.

1. THE ARROW ENDED IN SPACE
The rail from the web server stopped above the box CAPTION rather than at the
box, deliberately, so it would not draw through "THE MODEL". But that caption
is a wide-shot label: it is dark through every close-up, so for most of the
film there was an arrow pointing at nothing and a machine that looked
unconnected. The dodge was the bug.

The underlying problem is that a TALL box cannot wear its name across the top.
The rail comes down the column's midline and a title sized to be read at the
pull-back is about as wide as the box, so there is no "shift it aside" that
stays legible — the previous version was clamped to exactly the box width.
PipelineBox gained title_side="side": the name runs down the margin, reading
bottom-to-top. A tall box has height to spare, a column layout leaves its side
margins empty, and the midline comes free. It also balances the return label,
which was already a rotated label in the opposite margin. The rail now ends on
the frame edge.

SHOT_WIDE retuned 57.8 -> 55.0 to match: the box no longer needs headroom.

2. HALF-BOXES IN FRONT OF THE WORDS
The space marker, "␣It". It was drawn on purpose — whitespace belongs to the
token, which is true and worth knowing — but on screen it does not read as a
distinction, it reads as a broken glyph. It needs narration to land and a chip
in a silent 30-second cut has no chance to explain itself. display_token now
trims by default and takes show_space=True for a shot that says out loud what
the marker means. Chips still CARRY the full token; only the drawing changed.

3. RAGGED BARS
label_width capped the label but the track was placed next_to() it, so every
row put its bar wherever that row's word happened to end — "It" and "When"
gave different left edges. label_width now reserves a column and the row is
built against a fixed origin, so labels, tracks and values each share one edge.

WORTH KNOWING
The snapshot gate did not catch (3). A 16x16 luminance signature at tolerance
0.02 cannot see a bar moving a third of a unit. The five affected baselines
were re-approved after looking at the render. Not proposing a change to the
gate — it is protected, and a tighter one would be fragile — but it is a known
blind spot: alignment is a job for a structural assertion, and there are now
two of those.

183 tests (7 new), 20 baselines (5 re-approved), Evaluator green on all five
gates. assets/ refreshed.


================================================================================
SESSION 8 - a second video, and two components to carry it
================================================================================

Built videos/chatgpt_request_lifecycle: a 45-second continuous shot through the
whole request lifecycle - client, bot check, edge, gateway, orchestrator, then
the four-bay inference stack, then streaming and the after-response fan-out.
Ten stations on four bands in one closed circuit, pulled back to 4.07x at the
end.

This is a NEW video directory, not an edit of chatgpt_message_journey. That
film is committed in assets/ with every feature passing; rewriting it into a
nine-station 45s cut would have destroyed a reviewed deliverable. The two share
the /lib vocabulary, the palette, and the four in-box station accents on
purpose, so the new one reads as the same world seen wider.
videos/chatgpt_message_journey/ is byte-identical to where session 7 left it.

WHAT LANDED IN /lib

- lib/components/checks.py - CheckRow / CheckList. Rows of named checks that
  tick from pending to passed. Four call sites in this film alone (edge,
  gateway, stream, after), which is well past the promotion bar. Two things in
  it are scar tissue rather than taste: the three columns are RESERVED rather
  than measured (session 7's ragged-bar bug, in a new component), and the tick
  is two Lines with no fill at all, so it is shown and hidden on STROKE
  opacity. A blanket set_opacity would draw it as two filled slivers - the same
  fill-vs-stroke trap as the glow halos in session 5 and the Lucide icons in
  session 6. Third time. The docstring says so.

- lib/components/stacked.py - SegmentedBar. A proportional bar cut into named
  segments, with a legend or inline labels. Two load-bearing call sites: the
  orchestrator's prompt breakdown and the KV-cache split.

  min_segment is the whole reason it exists. Seven tokens out of 3,937 is 0.18%
  of the bar: sub-pixel, so the segment does not render, so the beat - "your
  question is the last few tokens of about four thousand" - fails silently with
  the bar looking like it has one fewer part than the legend claims. Each
  segment's DRAWN fraction is floored and the rest renormalised; the values stay
  true and `fractions` reports the unclamped arithmetic. Same trade as
  display_token's show_space, and documented as one: in a silent cut legibility
  beats notation, and the model keeps the real number either way.

- lib/components/probability.py - ONE new keyword argument, value_format. A
  quota meter and a batch-utilisation meter are a ProbabilityBar with a
  different number format, so that is a parameter, not a gauges.py (AGENTS rule
  1). The default is byte-identical to the f-string it replaced; the snapshot
  gate was run before and after and all 20 existing baselines pass untouched.
  Also documented the real hazard on set_value: it REPLACES value_mob, so the
  submobject list changes. It was probed on a throwaway scene before being
  relied on - .animate.set_value does interpolate (Manim aligns the two glyph
  trees) and the number morphs rather than ticking, which over 0.9s reads as a
  meter filling. The documented fallback was not needed.

Tests: 26 new structural tests in tests/test_components_structure.py (97 in
that file, 212 in the suite) and 3 new snapshot cases - check_list,
segmented_bar_legend, segmented_bar_inline - each rendered to a PNG and LOOKED
AT before approval. 23 baselines now. The legend case was re-shot once: at
scale 1.5 the composition was 14.6 units wide and the values were cut off the
right-hand edge of a 14-unit frame.

WHAT THE FRAME REVIEW CAUGHT THAT THE EVALUATOR COULD NOT

Seven real bugs, all found by rendering at the draft profile and reading a
contact sheet and full-size stills. Two are worth repeating here because they
are /lib behaviour rather than this video's:

1. ChatWindow.post -> commit -> _scroll_into_view -> _hide_scrolled_off calls
   set_opacity(1.0) on every message still in view. That UNDOES the hidden
   state a StreamingBubble is built in, so the finished reply was legible from
   the moment it was posted - ten seconds before it was supposed to arrive -
   and every later reveal(n) was a no-op nobody could see. The new scene calls
   reveal(0) immediately after post. chatgpt_message_journey has the same latent
   problem and was NOT changed: it is shipped and reviewed, and fixing it means
   re-rendering and re-reviewing that film. Worth a session of its own.
2. A chat window sized for one bubble silently eats the other. At CHAT_H=6.4 the
   message area is 3.29 units and question + reply need 3.47, so the transcript
   scrolled and hid the question at exactly the moment the answer landed. That
   is ChatWindow behaving as documented; the fix is to size the window against
   `message_area_height` rather than by eye, which is now measured in a comment
   in lifecycle_set.py.

The other five (a black frame mid-shot where the camera swung across 25 units of
empty world, the chat sliced by the frame edge on the travel shot, a caption and
a meter placed under the same bay on top of each other, a fade-out and an
entrance sharing one slot for 1.2s, and an emphasised segment with nothing
naming it) are in the video's own progress log.

None of the seven was visible to any of the five gates. This is the argument for
the review loop, stated again with fresh evidence.

STATE
Evaluator green on all five gates for --video chatgpt_request_lifecycle.
212 tests, 23 baselines, 0 existing baselines re-approved.
assets/chatgpt_request_lifecycle.mp4 committed at the final profile (1080p60,
44.1s, 4.1MB - vector art on a flat background compresses far better than
assets/README.md's rule of thumb assumes; the note there has been updated rather
than the rule quietly broken).

KNOWN-WEAK, RECORDED RATHER THAN HIDDEN
lib/components/probability.py predates lib/typography.py and still sizes its
text in absolute theme points, so at this film's shot widths an unscaled
ProbabilityBar's value text lands just under typography.MIN_READABLE. The scene
compensates by scaling the whole widget. That is a workaround, not a fix; the
fix is a frame_width argument, and it moves the probability_chart baseline, so
it is tracked as `probability-typography` in the video's feature list rather
than smuggled into this session.

STILL BLOCKED ON A HUMAN (eighth session)
1. harness-hook-bash-false-positive. Hit again, twice: the hook blocked
   `python scripts/approve_baselines.py --dry-run 2>&1 | head -30` purely
   because the command names a protected path and contains a redirect. Dropping
   the redirect worked. Nothing was routed around - the workaround is to drop
   the pipe, and every file whose text names the Evaluator was written with the
   Write tool.
2. harness-ci.
3. default branch.

================================================================================
SESSION (ninth): two /lib defects in lib/components/stacked.py
================================================================================
Both found by an independent review of chatgpt_request_lifecycle, both in /lib
rather than in the film, so both will follow every future caller.

1. THE LEGEND DID NOT RESERVE ITS NAME COLUMN, AND THE DOCSTRING SAID IT DID
   _build_legend computed value_width = value_mob.width INSIDE the row loop, so
   it was that row's own value width, not a reserved column. A row whose value
   was "7" therefore handed its name more room than a row whose value was
   "2,400", and utils.fit_text shrank only the latter. Measured on the film's
   own data, the four name heights came out 0.1369 / 0.1155 / 0.1687 / 0.1766 —
   a 53% spread, not even monotone in name length, and visible at 1080p. This
   contradicted the module docstring's "Column discipline" section and the
   inline comment three lines below it.
   Fixed: the value column is measured once over all rows before the layout
   loop and reserved for every row.
   Why the existing test missed it: tests/test_components_structure.py's
   test_legend_rows_share_a_left_edge_and_a_value_edge asserts left and right
   EDGES, and those stay perfectly aligned with the bug present — the ragged
   part is where the NAME stops, not where the row does. The 16x16 luminance
   snapshot cannot see a type-size change either. A new test asserts that two
   rows carrying the SAME name but values of very different widths render that
   name at the same size (identical strings on purpose: ascenders and descenders
   move a Text's bounding box around, so comparing different strings would
   measure the font rather than the layout). It fails on the old code — 0.1915
   against 0.1576.

2. TWO CONSTANTS FOUGHT IN SILENCE, AND THE LABEL LOST
   A caller who passed min_segment below INLINE_MIN_FRACTION in
   labels="inline" mode got the worst of both rules: the sliver was floored up
   so it would be DRAWN, and then _build_inline dropped its label because it was
   still under 12% of the bar. That is how the prefill beat of
   chatgpt_request_lifecycle shipped with the number it exists to show missing.
   Two changes:
   a. The constructor now REFUSES that combination, with a message naming both
      constants and both ways out. Dropping a too-narrow label is still correct
      behaviour, so the opt-out allow_unlabelled_segments=True keeps it for the
      case where a caption elsewhere names the sliver — tests/snapshot_cases.py
      and the drop test now say so explicitly.
   b. An inline label is never shrunk below typography.MIN_READABLE. Raising
      min_segment alone did not fix the film: fitted to 94% of its segment the
      label measured 0.0083 of frame height against a 0.020 floor, i.e. drawn
      and illegible, which is the dropped-label bug with extra steps. A label
      may now overhang its own segment, and a cursor pushes each label clear of
      the previous one so they can never overlap each other.
   Both behaviours have tests. No baseline moved: the inline snapshot case keeps
   its dropped label, and the legend case's rows keep their edges.

================================================================================
SESSION (tenth): chatgpt_request_lifecycle taught autoregressive generation wrong
================================================================================
Video-level work, logged in full in videos/chatgpt_request_lifecycle/
claude-progress.txt. The parts that matter to the HARNESS and to /lib:

1. A GREEN GATE CANNOT SEE A RAIL THAT WAS NEVER DRAWN.
   lib/components/factory.PipelineBox fills its frame with theme.BG at full
   opacity. Any rail added to a set BEFORE the box is buried by it. This film
   had four such rails - the descent through the column, the three between the
   bays, the way out - invisible since it shipped, and the new loop-back rail
   joined them: the final render showed a runner lapping a path with nothing
   under it while all five Evaluator gates were green. Found by probing pixels
   at the rail's computed x, not by reading code. Fixed in the set's add order.
   Worth knowing for every future set with an enclosure: draw order is
   load-bearing and nothing checks it.

2. COMET TAILS AND THE PULL-BACK DO NOT MIX.
   effects.comet samples once per frame and welds the samples with straight
   chords. At pull-back distance a payload crossing 74 world units in 0.4s puts
   3 units (12 at the draft's 15fps) between samples, so the trail stops being a
   comet and becomes a straight line across the set that cuts every corner - the
   exact appearance of the routing bug lib/routing.py exists to prevent, with no
   routing bug present. routing.assert_trail_clears passes, because the corridor
   it cuts across happens to be empty. The rule that came out of it: below about
   0.4s over a long path, either cut dissipating_time to ~3 frames or drop the
   trail entirely - a bare dot is sampled onto the true path every frame and
   cannot cut a corner at all.

3. A /lib OFF-BY-ONE, FOUND AND NOT FIXED.
   lib/components/chat_ui.StreamingBubble maps each word to a glyph slice by
   len(word) over the concatenated words, but Pango rendered 48 glyphs for this
   film's 49 characters, so the LAST word's slice is short by one. Invisible in
   this cut. It is a /lib defect with a test and a baseline behind it and it
   deserves its own session rather than a drive-by; logged here so it is not
   re-discovered from scratch.

STILL BLOCKED ON A HUMAN (tenth session)
1. harness-hook-bash-false-positive. Hit four more times. Every command that
   both names a protected path and contains ANY redirect is refused - including
   `... | tail -20`, `... 2>&1`, and a heredoc whose BODY merely mentions
   scripts/evaluate.py. The workaround each time was to split the command; the
   file that documents the Evaluator was written with the Write tool. Nothing
   was routed around.
2. harness-ci.
3. default branch.

----------------------------------------------------------------------
SESSION 11 — /lib: the legend is one type size, and it can sit under the bar.

THE DEFECT
  In lib/components/stacked.py::_build_legend the NAME column went through
  utils.fit_text and the VALUE column never did. A legend too narrow for its
  names therefore came out with the numbers at full size and the words at a
  third of it. Measured in scene context on the orchestrator beat of
  chatgpt_request_lifecycle, after station.fit at W_SERVICE=16.0:

    row                name/frame-h   value/frame-h
    system prompt         0.0077         0.0251
    tool definitions      0.0066         0.0251
    memory + prefs        0.0075         0.0215
    your message          0.0070         0.0208

  typography.MIN_READABLE is 0.020. Every number cleared it; not one word did.
  This is the same "drawn and unreadable" failure _build_inline already guards
  against, one column over. Neither gate could see it: the row edges stayed
  perfectly aligned (the structural alignment test passed) and a 16x16
  luminance signature cannot tell small type from large (the snapshot passed).
  It is also NOT the bug the previous fix in this function addressed — that one
  made the names equal to EACH OTHER by reserving the value column once, and it
  is preserved. This one is names against VALUES.

BUILT
- stacked.py: the legend is sized as one block. The row is
  swatch + PAD_SM + name + PAD_SM + value, of which only the two text columns
  can scale, so the shared factor is solved once and applied to names and
  values alike rather than iterated. Nothing is ever scaled up.
- stacked.py: legend_side="below" (LEGEND_SIDES). The table sits under the bar
  sharing its left edge, and legend_width defaults to the bar's length, so the
  numbers right-align with the bar's end and read as a key to it.
- stacked.py: strict_legend=True refuses a legend that would be typed below
  MIN_READABLE, in the tone of the constructor's existing inline refusal —
  it names the constant, the width the row actually needs, and three ways out.
  Off by default, so no existing caller changed behaviour.
- tests/test_stacked_legend.py: 7 tests. Six fail against the pre-fix
  component; the headline one fails for the right reason (name scales
  0.273-0.320 against value scales of 1.0).
- tests/snapshot_cases.py: segmented_bar_legend_below, a new approved baseline,
  looked at before approving.

WHAT DID NOT NEED DOING
- The plan expected segmented_bar_legend to move. It did not, and neither did
  segmented_bar_inline. That case builds a 5.0-long bar at frame_width 13.6,
  where the legend has 4.25 units against the 3.34 the row needs, so neither
  the old fit_text nor the new shared factor shrinks anything. Byte-identical
  output. Worth knowing: the existing baseline never covered the cramped case
  that was broken, which is why the new case was added.
- The plan recommended fading the `sources` icon chips to buy vertical room.
  Measurement said it was not needed: with the legend below the bar the content
  is 4.90 x 2.35 against a 4.90 x 2.30 slot, a fit factor of 0.980, and every
  row lands at 0.0216-0.0286. Two cheaper adjustments bought the room instead
  (chips gutter PAD_MD -> PAD_SM, which was what made `fit` width-bound; group
  buff PAD_SM -> PAD_XS). No choreography change, no re-timing, no runtime cost.

STILL BLOCKED ON A HUMAN (eleventh session)
1. harness-hook-bash-false-positive. Hit twice more; both times the command
   merely piped the Evaluator's output to `tail`. Split each time. Nothing was
   routed around.
2. harness-ci.
3. default branch.
4. The StreamingBubble off-by-one from session 10 is still open.

REVIEW FOLLOW-UP: a false positive in the new strict_legend guard
  Found by the independent reviewer of the legend change, confirmed by
  reproduction, fixed here. /lib only; no frame changed, runtime untouched.

  THE BUG. `_build_legend`'s strict_legend guard measured each drawn label with
  typography.measure(), which is a BOUNDING BOX. A bounding box depends on
  which glyphs are in the string, not on how large the type is. Names with no
  ascender, no descender and no capital - "sources", "runs", "errors" - measure
  their x-height, 0.0174 at role micro, which is under MIN_READABLE=0.020 at
  scale 1.0 NO MATTER HOW WIDE THE LEGEND IS. Reproduced: a bar with 8.0 units
  of legend for a row needing 1.84 raised UnreadableTextError with nothing
  scaled at all. Worse, the message then advised widening a legend already four
  times wider than necessary; of the four escapes it named, three could not
  help and the fourth was to switch the guard off. The film's own four names
  pass only because they happen to carry descenders - "memory + prefs" has a
  'y', "system prompt" a 'p'. A rename to a word like "sources" would have
  failed the film at construction, and strict_legend=True is live on that bar.

  THE FIX. The guard now tests the TYPE SIZE - typography.fraction(LEGEND_ROLE)
  times the shared factor actually applied - and not any mobject's box. That is
  what legibility turns on, so the guard can only fire when something really
  was shrunk (below scale 0.909 for role micro against a 0.020 floor). The
  message now prints the scale applied and the room needed against the room
  available, because "too narrow" without a number is not actionable.
  LEGEND_ROLE is a new module constant, and both legend columns are now drawn
  through it: if the drawn role and the checked role ever drift apart the guard
  silently starts grading the wrong thing.

  NOTE ON typography.measure. Its docstring says the box "errs toward passing
  borderline text rather than failing readable text". That is true for a string
  with ascenders or descenders and FALSE for an x-height-only one, which is
  what this guard walked into. Worth knowing before building another check on
  it - measure() is right for auditing a rendered shot, and wrong as a
  construction-time gate on type size.

  TWO TESTS, both failing against the guard as shipped in a975bb8:
    - ...does_not_refuse_a_legend_with_room_to_spare: every name x-height-only
      on purpose, and it asserts they DO measure under the floor by box, so the
      test cannot quietly stop testing anything if the font changes.
    - ...still_refuses_a_legend_that_really_is_crushed: the other half, so the
      fix cannot be mistaken for defanging the guard.

  ALSO, from the same review:
    - test_the_orchestrator_beats_legend_is_readable_after_the_station_fits_it
      fitted VGroup(bar) alone. The scene fits VGroup(sources, bar), and the
      chips row is what SETS the fit factor - at PAD_MD it measured 5.207
      against a 4.90 slot, so fit was width-bound on three badges. The one test
      guarding the post-fit contract was proving it by accident and would have
      stayed green if a later edit to the chips put the names back under the
      floor. It now builds the real chips row.
    - Two measured figures were wrong in prose and are corrected: the scene
      comment said the bar draws 4.56 after fit (it is 4.80), and the note
      below said the segmented_bar_legend row needs 3.21 units (it needs 3.34).
      Neither changes a conclusion; both are the kind of number someone later
      takes as measured fact.

  STILL OPEN, from the same review, NOT addressed here
    - No snapshot baseline exercises the shared-scale path: both legend cases
      render at scale 1.0, so they guard placement, not the arithmetic. The
      only coverage of the scaled path is one unit test.
    - The guard cannot see Station.fit - it runs at construction, and fit
      happens afterwards on a group the component has no handle on. A post-fit
      regression is caught only by the one test above.
    - The orchestrator bay now has zero vertical slack (post-fit content 2.300
      against a slot cap of 2.300). A fifth segment or a second caption line
      has nowhere to go and would have to take it from the chips.
    - The worst row clears the floor by ~8% (0.0216 vs 0.020).
    - Pre-existing, unrelated to this change: the "input classifier" badge's
      opaque fill buries the bay's bottom rail for the badge's height. Same
      occlusion class as the buried-rail bug logged in the tenth session.


SLIDES SPIKE BROUGHT UP TO THE CURRENT FILM (experiments/ only)
  Merged claude/experiment-manim-slides into this branch so the click deck sits
  on the film as it ships now - the fifteen narration holds, the rebuilt
  orchestrator bar and its legend. Only .gitignore was touched on both sides
  (both added "slides/"); resolved to one line. No shipped code changed:
  assets/chatgpt_request_lifecycle.mp4 is still 59.398698s and all five
  Evaluator gates are green.

  THE PROPERTY THAT MADE IT A MERGE AND NOT A REWRITE. SlidesLifecycle
  subclasses TheLifecycle and runs its beat methods unchanged, so it picked up
  both commits by inheriting. No choreography was forked into the deck, and the
  film was not re-timed to suit it.

  THE HOLDS COST NO STOPS. A bare self.wait() is a pure-Wait play and those are
  filtered out of splitting, so a hold extends the slide that is already open
  rather than making a click. Still 97 animations -> 70 stops. Durations moved:
  final profile 0.183-2.399s, median 0.80s, 59.399s total, which is the film's
  runtime exactly. Fourteen of the fifteen holds follow a content play;
  the fifteenth is beat_pull_back's closing 0.5 -> 0.9, which is the one the
  deck WANTS after a clear-down because it is the last play of the film and
  carries the final stop past the caption's fade. Each hold's slide and resting
  frame is tabulated in experiments/README.md.

  THE ORCHESTRATOR BAR. Four fill stops (one per segment) plus the emphasise
  stop, each resting on a filled state; the legend's four names are readable at
  both profiles now that it sits below the bar. The README previously called
  the fill loop itself "five stops" - the loop is range(1, len(bar)+1) over
  four segments and always produced four. Corrected there.

  ALSO CORRECTED. Every scene_lifecycle.py line reference in the spike's README
  and docstrings pointed at pre-holds line numbers; all of them were remapped.

  OPEN. The deck is longer to click through than it was, and whether the holds
  help or hurt a click deck is a judgement for a viewer: a hold is dead time in
  a medium where the presenter already controls the pace. Nothing was changed
  either way - the film's timings stay the single source of truth.

--------------------------------------------------------------------
SESSION — graduated the click deck from spike to enforced convention.

  WHY. The user asked for docs about the slides work AND for the harness to
  require it: "all videos created should be having this slides conversion as
  well or they should be created with both the slides and video in mind so
  that the conversion is seamless."

  THE SPLIT. experiments/slides_lifecycle/slides_scene.py was 474 lines welded
  to one film. Generic half -> lib/slides.py (ClickDeck): INTRODUCING /
  NOT_INTRODUCING, _is_clear_down, _is_pure_wait, no_stops(), sticky_stops(),
  _sticky_play(), the play split state machine, and construct() which calls the
  film's own construct so a deck never restates the beat order. Specific half
  stays with the video: videos/chatgpt_request_lifecycle/slides.py is 102 lines
  (LOOP_ON_ANIMATION + _is_loop_target + _token_cycle + the counts).
  videos/chatgpt_message_journey/slides.py is 66 lines, ~25 of code.
  experiments/slides_lifecycle/ is deleted; experiments/README.md records the
  graduation.

  REGRESSION BAR HELD EXACTLY. 97 animations -> 70 stops, 5 suppressed regions,
  loop on animation 3 (slide 4 in the config). Rendered chunks at 480p15:
  0.200-2.667s, median 0.833s, 61.597s of chunk video, no single-frame stop.
  Those are the spike's numbers to the frame. assets/chatgpt_request_lifecycle
  .mp4 is untouched at 59.398698s; neither film's scenes/ was edited.

  ENFORCEMENT WITHOUT TOUCHING THE PROTECTED GATE. tests/test_slides_convention
  .py is picked up by the existing unit gate (it runs pytest over tests/), so
  no gate name and no harness.json change. It asserts, per video with a scene
  in its manifest: a slides.py exists with exactly one ClickDeck subclass; the
  deck subclasses a scene from scenes.json with ClickDeck ahead of it in the
  MRO; the deck defines no construct of its own; no stop is zero-length or a
  single frame at the render profile's fps; the deck's total time equals the
  film's; the declared counts still hold. Verified it fails by moving a
  slides.py aside - three failures, all naming the missing deck.

  NOTHING IS RENDERED BY THE TEST. lib.slides.probe runs a deck's construct
  with the frame loop replaced by begin/finish/clean_up per animation, so the
  whole split state machine executes with no rasterisation. That is also
  scripts/build_slides.py --probe, the inner loop. Cost: the unit gate went
  6.5s -> 51s, because the probe still builds two whole films' mobjects. That
  is the price of checking the split at all without a render, and it is two
  orders of magnitude cheaper than one.

  THE SECOND FILM IS THE REAL TEST, AND IT FOUND SOMETHING. chatgpt_message
  _journey converts: 43 animations -> 33 stops, chunks 0.200-1.933s, median
  0.600s, no single-frame stop, final stop on the whole plant with the complete
  reply. It needed ONE new generic mechanism, not a special case: _sticky_play,
  a predicate form of sticky_stops for a ramp written inline in a beat the deck
  may not edit. The lifecycle deck's ramp is six calls to a helper it can wrap;
  the journey's is eight plays inline in beat_transform with nothing to wrap, so
  the plays are recognised by what they animate (a single .animate on a
  LayerBlock) instead. Both now sit in lib/ and both films use only lib/.

  ONE CONVERSION DEFECT, REPORTED NOT WORKED AROUND. scene_factory.py:348-354
  plays camera.focus + FadeOut(courier) + answer.body.animate.set_opacity(1.0).
  A .animate opacity raise is not a recognised arrival (a _MethodAnimation does
  not advertise its direction), so the play is classified as a clear-down and
  merges forward into the three-word reveal after it. The deck loses one click.
  The resting frame was extracted and is fine - the chat window with "It turns
  your" in the bubble - so this costs a click, not a blank slide. The film was
  NOT edited; the rule it breaks is written up in docs/slides.md 3.4 and in the
  deck's own docstring. The fix, if a human wants it, is FadeIn(answer.body) in
  the film.

  DOCS. docs/slides.md: how the splitting works (the four load-bearing
  behaviours, with the frame each was found by), the per-repeat call table for
  both films, eight authoring rules, the build commands, and the Reveal.js
  offline-cache note (six files for Reveal 6.0.1 under
  ~/.cache/manim-slides/). Wired in: AGENTS.md Map rows for lib/slides.py,
  videos/<slug>/slides.py and scripts/build_slides.py; Generator rule 7; a
  bullet under "Continuous shots"; the factory-video skill gained a "Two
  outputs, one choreography" section and a deck step in its build loop; both
  per-video AGENTS.md; scripts/new_video.py scaffolds a slides.py stub and says
  so on stdout.

  GATES. Five green on both videos (lint, import, unit, snapshot, render).

  OPEN / WEAK.
  * The unit gate is now 51s, nearly all of it the two probes. If that becomes
    a problem the honest fix is to drop the "deck total == film total" check,
    which is what costs the second pair of probes.
  * The .animate-arrival blind spot is still a blind spot. It is now
    documented, and one real instance of it exists in a shipped film. A
    predicate that could see it would need to compare a _MethodAnimation's
    target state, which nothing in manim exposes cheaply.
  * EXPECTED_ANIMATIONS/EXPECTED_STOPS are enforced now rather than warned
    about. A legitimate choreography change therefore fails the unit gate until
    the deck's constants are updated - deliberate, but it is a step a future
    session must not paper over without looking at the deck.

---

## Session — Arc 1 Ep 1: the `tokenization` video, built

Built `videos/tokenization/` from `docs/briefs/arc1/tokenization.md`: the set,
four local props, the seven-beat continuous scene, the deck, both manifests.
Evaluator green including `render`. Measured **59.500 s at `final`** (3570 frames
at 60fps) against the hard 60.0 s cap; `assets/tokenization.mp4` is that cut,
4.2M. Deck: 45 animations -> 40 stops, total 59.500000 s, one suppressed region.

Every beat's `run_time`s sum EXACTLY to the brief's beat length, checked
statically as well as by the probe: 5.00 / 6.50 / 11.00 / 11.50 / 6.50 / 10.50 /
8.50.

Nothing was promoted to `/lib`. `CharacterRow`, `MergeTable`, `VocabWall` and
`GhostCircuit` are local one-offs with no second consumer, so the AGENTS.md
promotion rule says they stay in the video. `CharacterRow` is the likeliest
future promotion and is flagged `planned` in the video's `feature_list.json`.

**Five repo-level hazards this build hit, all recorded in
`videos/tokenization/claude-progress.txt` with the frame that caught each:**

1. **`scenes/props.py` cannot be imported by bare name any more.** Every video
   has one; the deck route puts each video's `scenes/` on a FLAT `sys.path`; and
   `tests/test_slides_convention.py` builds all three films in one pytest
   session. So the first `props` imported wins the name for the process, and
   this film's set failed with a name error about a class in its own directory —
   passing in isolation and failing in the suite. `tokenizer_set.py` now loads
   it by file location under a unique module name. **A fourth video needs the
   same treatment**, and a `lib` helper for it would be a reasonable harness
   item if a fifth appears.
2. **`FadeIn` animates UP TO a mobject's current opacity.** Anything built at
   opacity 0 fades in to 0. `lib/components/chat_ui.StreamingBubble` builds its
   text that way by design (it expects `reveal()`), and beat 0 streamed an
   entirely EMPTY bubble through a whole draft render with every gate green.
3. **An animation adds its mobject to the scene's TOP level.** Wrapping
   already-added children in a fresh `VGroup` to fade them out draws them twice
   and leaves the originals on screen. Clear-downs must fade the same objects
   the arrival added.
4. **Pango lays a `Text` out against the RENDER's pixel width, not the frame's.**
   A 45-unit caption at a 60-unit shot wrapped to two lines at 480p and would not
   have at 1080p — i.e. a caption whose shape depends on the profile it was
   reviewed in. Long wide-shot captions need explicit line breaks.
5. **`PipelineBox`'s title is sized for the pull-back and hangs OUTSIDE its
   frame**, so any close-up of the box crops it. It needs the same close-up /
   wide-label cross-fade a `Station` does for itself.

Also probed and **not** reproduced: the standing warning that `.animate.scale` on
a container `VGroup` drags its children's opacity down. At manim 0.21 it does
not, which is what let beat 6 shrink the whole set into the ghost node while the
camera holds still.
